# Qleverfile for DOOS
#
# qlever get-data  # downloads .zip file of size 13 MB, uncompressed to 323 MB
# qlever index     # takes ~10 seconds and ~1 GB RAM (on an AMD Ryzen 9 5900X)
# qlever start     # starts the server (instant)
# removed from ui section: UI_CONFIG = odis

[data]
NAME              = doos
BASE_URL          = http://doos.earthcube.org
GET_DATA_CMD      = mc cp --recursive decoder/decoder/graphs/latest/ ./data-decoder
DESCRIPTION       = DOOS Ocean metadata ${BASE_URL}
TEXT_DESCRIPTION  = DOOS release graph description
FORMAT            = nq

[index]
INPUT_FILES     = ./data-doos/*
CAT_INPUT_FILES = ( cat ./data-doos/*.nq )
SETTINGS_JSON   = { "ascii-prefixes-only": false, "num-triples-per-batch": 100000 }

[server]
PORT               = 7019
ACCESS_TOKEN       = ${data:NAME}_7643543846_6dMISzlPrD7i
MEMORY_FOR_QUERIES = 25G
CACHE_MAX_SIZE     = 20G
TIMEOUT            = 240s

[runtime]
SYSTEM = docker
IMAGE  = docker.io/adfreiburg/qlever:latest

[ui]
